#!/usr/bin/env bash
# wireguard-vps-installer — one-shot install (standalone, no external deps)
# This single file includes all lib/ functions inline for curl | bash compatibility.
set -euo pipefail
# shellcheck disable=SC2154,SC2155

